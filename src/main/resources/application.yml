spring:
  datasource:
    url: jdbc:mysql://localhost:3306/smart_logistics?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: 20051112
    driver-class-name: com.mysql.cj.jdbc.Driver
  thymeleaf:
    cache: false
  servlet:
    multipart:
      max-file-size: 10MB

server:
  port: 8080

mybatis:
  mapper-locations: classpath:mapper/*.xml

logging:
  level:
    com.example.logistics: info
